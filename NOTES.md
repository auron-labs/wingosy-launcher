- when syncing or adding romm, the "connected" icon in the settings menu
  switches back and forth super fast
- clicking play on cuphead caused "No RetroArch core is configured"; 1. it's not
  a game that plays in retroarch 2. it should ask the user if it should
  auto-install one if there's once choice 3. the alert stays on every page with
  no way to dismiss it
- opening settings in windowed mode places the settings inside the home shell,
  causing the normal sidebar to still show
- "sync updates & dlc" is there, but no way to know if everything is already
  installed, or if it's up to date
- i installed eden, but clicking play on cuphead just makes the alert dissappear
  and re-appear instantly
- switch is showing that retroarch is a valid emulator, when it's not
- controller mapping/setup didnt work for eden
- filters dissappeared from the ui

