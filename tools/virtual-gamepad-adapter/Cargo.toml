[package]
name = "wingosy-virtual-gamepad-adapter"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target."cfg(windows)".dependencies]
vigem-rust = { version = "0.2", default-features = false, features = ["x360"] }
