## Immersive Mode

- pushing B on controller exits fullscreen
- no way to change platform using controller
- can't navigate settings page at all
- settings says romm is connected, but clicking sync says "Pair Wingosy with
  RomM or connect with a client access token first."
- after pairing, it says "click sync library", but we should just automatically
  sync it
- keybinding bar should be at the bottom
- `RomM detailed metadata contains no Switch files categorized as update or dlc; verify the child-file categories, then retry Choose “Sync Updates & DLC” to retry after correcting the issue.`
  this is a horrible message for a user to see, I'm a developer and I don't even
  know what it means
- `Select Manage cached saves in this game's More options menu to jump here when cloud saves are configured on the server.`
  why is this even on the game page?
- Manage cached saves does nothing when you select it
- when viewing a game, the platform sidebar goes empty instead of just hiding it
- `Wingosy update & DLC files are current` is randomly next to the sync button?
- Hitting start+select caused Eden to say "are you sure you want to leave",
  which I think it correct, but you can't use the controller to change it from
  yes to no, you can hit B to hit yes though.
- after going back from a game to the menu, you can't move the selector from
  left to right sometimes

